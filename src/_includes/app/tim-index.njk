<!-- tim-index.njk
  --
  -- Anzeige der TIM Server
  -->

<!--
		"S100": {
			"sid": 100,
			"type": "p",
			"name": "RB-Ost-Südost",
			"fqdn": "tim-dbsus-rbostsuedost.ga.itbf.db.de"
-->
			
<div class="w3-container">

	<section class="w3-row-padding">
		
		{% for data in tim.Server %}
			<article class="tim-server w3-col m12 l6 w3-padding" data-fqdn="{{ data.fqdn }}">
				
				<div class="w3-card-4">
					<header>
						<a href="{{ data.url }}" title="{{ data.hint | safe }}">
							<div class="tim-status w3-container w3-padding w3-yellow w3-hover-shadow caption" data-fqdn="{{ data.fqdn }}">
								<b>{{ data.name | d('&nbsp;') | safe }}</b>
								<span class="w3-right">{{ data.sid | d('&nbsp;') | safe }}</span>
							</div>
						</a>
					</header>
					
					<div class="w3-container w3-white w3-hide-small">
						<p class="" style="overflow:hidden;">
							<a href="{{ data.url }}" title="{{ data.hint | safe }}" class="w3-margin-bottom ">
								{{ data.fqdn }}
							</a>
						</p>
					</div>
				</div>
			</article>
		{% endfor %}
	</section>	
</div>
